server:
  port: 8000
spring:
  application:
    name: Fraud
  datasource:
    url: jdbc:postgresql://localhost:5433/fraud
    username: amigoscode
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true